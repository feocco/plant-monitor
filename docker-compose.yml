services:
  plant-monitor:
    build: .
    container_name: plant-monitor
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./plants.yaml:/app/plants.yaml:ro
      - ./data:/app/data
    ports:
      - "${SERVICE_PORT:-8088}:${SERVICE_PORT:-8088}"
